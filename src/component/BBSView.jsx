import React from 'react';
import Messages from './Messages';

export default function BBSView() {
    return (
        <div position="relative">
            <h1>React掲示板 (例)</h1>
            <Messages />
        </div>
    );
}